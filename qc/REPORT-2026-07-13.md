# QC Milestone Report — 2026-07-13

Branch: `overnight/2026-07-04` (4 ahead of `qa/litlit-qc`, 0 behind — clean fast-forward available).
Previous report: 2026-06-18 (25 days — this report was overdue).

## Suite status

| Check | Result |
|---|---|
| vitest full suite | **302 passed / 0 failed** (11 files, 34s) |
| tsc — server + shared + drizzle (production scope) | **clean** |
| tsc — full project incl. client | not runnable in this harness (see Environment); no client files changed since last clean check (2026-07-04) |

Baseline drift since 06-18 report: 300 → 302 tests (+3 from 06-19 params-wiring sessions, −5 legacy galleryImages removal on 07-04, +2 retry tests 07-04... net path: 300→303→291→302). No test was ever left red between sessions per sprint-log; the 291→302 delta this run is +11 new invariant tests.

## Regressions

None found. Full suite green. The 07-04 session's two hardening commits (`7e00efd` four bug fixes, `97aa6b7` OCR/DALL-E in-process retry) hold up: retry classification (`isRetryableImageGenError`) and `withRetry` wiring are exercised and passing.

## New-feature validation (this run)

`server/storyBibleBoundary.test.ts` (10 tests) + 1 appended to `scenePipeline.test.ts` — dedicated coverage for the repo's HARD INVARIANT (OCR transcription decoupled from image generation; story bible mediates):

- Bible built exactly ONCE per book; every page prompt reuses it (no per-page rebuild drift).
- Locked-field drift check: art style, character visual descriptions, locations, faction markers, motifs are byte-identical across every page's prompt in a book (real promptService, mocked LLM).
- Chronology gate: page N never sees events from page ≥ N; un-numbered chronology entries always pass (documented current behavior).
- Bible-build failure degrades gracefully: all pages still process, book completes, prompts fall back to no-bible mode.
- Render boundary purity: `generateImage` receives exactly {prompt, keyPrefix, params} and NEVER raw OCR/page text — asserted in both page mode and scene mode.
- promptService units: null bible on empty/short input (no LLM call), null on LLM failure (no throw), empty-page fallback uses locked artStyle without an LLM call, per-page prompt failure throws (pipeline owns retry), batch API (`generateImagePromptsWithContext`) builds one bible and isolates per-page failures.

## Performance sanity

Test execution itself: 1.4s for 302 tests. Wall time is dominated by module collection over the Windows mount (collect 20.9s, prepare 6.7s) — environmental, not code. No test exceeds 650ms (pdfService real-PDF extraction). No new slow paths introduced; this run added no production code.

## Type-debt finding (new information)

Root `tsconfig.json` EXCLUDES `**/*.test.ts` — `npm run check` has never type-checked the test files. Checking them explicitly found pre-existing errors, now partially cleared:

- FIXED this run: `(x: never)` mockImplementation pattern (2 errors in scenePipeline.test.ts's setup, copied into the new file then fixed in both). Zero behavior change; suite re-run green.
- REMAINING: `server/core.test.ts` — 17 pre-existing TS2339 errors (`.pages`/`.items`/`.totalPrice` on `{}`), runtime-passing, tRPC result typing. Low-priority debt; fixing means typing the tRPC caller results. Needs no urgent action.

## Environment notes (for future runs)

- Shell calls are hard-capped at 45s each, run in isolated PID namespaces, and background processes DIE when the call returns. Nothing survives between calls. Full-project tsc (client included) exceeds the cap over the mounted FS and cannot currently be run here; scoped tsc (server+shared+drizzle) fits (~35s) via a /tmp tsconfig with absolute paths + explicit typeRoots. Full vitest fits (~34s).
- The Edit/Write file tools STILL truncate file tails on this mount (todo.md lost its last 3 lines during a 1-line insert; restored from git + patched via python). All file writes this session done via shell heredoc/python.
- git commit works again in this sandbox (June's unremovable `.git/index.lock` blocker is gone; stale-lock unlink warnings are cosmetic).

## What's left (next highest-value units)

1. Persist chosen image params on the book row so processPdf/retry reuse them — NEEDS JEROME (schema migration approval; open todo item).
2. `core.test.ts` type-debt cleanup (17 errors) — mechanical, ~1 unit.
3. Decide fast-forward of `qa/litlit-qc` to `overnight/2026-07-04` tip (4 commits) — Jerome reviews and pushes.
4. Durable job queue (v2.0, deferred by design).
